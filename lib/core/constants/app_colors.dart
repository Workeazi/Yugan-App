import 'package:flutter/material.dart';

class AppColors {
  // YUGAN Brand Colors
  static const primaryColor = Color(0xFF16A34A);
  static const secondaryColor = Color(0xFF22C55E);
  static const accentColor = Color(0xFF4ADE80);
  static const greenColor = Color(0xFF16A34A); // Alias for primary
  
  static const lightBackgroundColor = Color(0xFFF8FAFC);
  static const darkBackgroundColor = Color(0xFF0F172A);
  static const whiteColor = Color(0xFFFFFFFF);
  static const blackColor = Color(0xFF000000);
  
  // Base Colors
  static const favColor = Color(0xFFE91E63);
  static const redColor = Colors.red;
  static const transparentColor = Colors.transparent;
  static const greyColor = Colors.grey;
  static const lightBlueColor = Colors.lightBlue;

  // Cards & Layout
  static const lightCardColor = Color(0xFFFFFFFF);
  static const darkCardColor = Color(0xFF1E293B);
  static const lightBottomNavBarColor = Color(0xFFFFFFFF);
  static const darkBottomNavBarColor = Color(0xFF1E293B);
  static const lightProductCardColor = Color(0xFFFFFFFF);
  static const darkProductCardColor = Color(0xFF1E293B);

  // Instamart Theme Colors (Legacy/Fallback)
  static const instamartPrimary = Color(0xFF8A038C);
  static const instamartDark = Color(0xFF700070);
  static const instamartBackground = Color(0xFFF5F5F5);
}
